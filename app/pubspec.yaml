name: naduri_alimi
description: "나들이 알리미 — 아이와 주말 나들이, 새 행사 푸시 알림"
publish_to: "none"
version: 0.4.0+4

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.3.0
  url_launcher: ^6.3.0
  path_provider: ^2.1.0
  flutter_local_notifications: ^22.0.1
  workmanager: ^0.9.0+3
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
